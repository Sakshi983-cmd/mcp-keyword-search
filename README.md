# MCP Keyword Search Tool

A simple FastAPI-based MCP server that allows users to upload a text file and search for a specific keyword.

## 🚀 How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/Sakshi983-cmd/mcp-keyword-search.git
   cd mcp-keyword-search
